import { type ReactNode } from 'react';
import { ArrowDownRight, ArrowUpRight } from 'lucide-react';
import { Card } from '@/components/ui/Card';
interface StatCardProps { label: string; value: string; change?: string; changeLabel?: string; trend?: 'up' | 'down' | 'neutral'; icon: ReactNode; iconClassName: string; }
export function StatCard({ label, value, change, changeLabel, trend = 'up', icon, iconClassName }: StatCardProps) { return <Card className="p-4 sm:p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-medium text-slate-500">{label}</p><p className="text-xl sm:text-2xl font-bold text-slate-800 mt-2 tracking-tight">{value}</p></div><div className={`h-9 w-9 rounded-lg flex items-center justify-center ${iconClassName}`}>{icon}</div></div>{change && <div className="flex items-center gap-1 mt-3 text-xs"><span className={`inline-flex items-center font-medium ${trend === 'up' ? 'text-success-600' : trend === 'down' ? 'text-danger-600' : 'text-slate-500'}`}>{trend === 'up' ? <ArrowUpRight className="h-3 w-3" /> : trend === 'down' ? <ArrowDownRight className="h-3 w-3" /> : null}{change}</span>{changeLabel && <span className="text-slate-400">{changeLabel}</span>}</div>}</Card>; }
